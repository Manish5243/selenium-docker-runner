pipeline {
    // master executor should be set to 0
    agent any
    stages {
        stage('Run test') {
            steps {
                //sh
                bat "docker-compose up"
            }
        }
        stage('Grid down') {
            steps {
                //sh
                bat "docker-compose down"
            }
        }
    }
}